# Decision

## Initial decision

Create `universal-dictionary` as a dedicated repository instead of embedding naming authority inside each project.

### Reason

Shared naming control should be centralized so OpenClaw-managed projects can align over time.
